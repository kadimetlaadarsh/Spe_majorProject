PORT=4200
MONGO_URI=mongodb://localhost:27017/medical_scans

# Must match auth-service secret if verifying locally

# Where to store uploaded files (absolute or relative to project root)
FILE_UPLOAD_DIR=./uploads/scans
JWT_SECRET=replace_this_with_a_secure_secret
